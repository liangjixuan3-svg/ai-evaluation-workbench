# 发布与复测实施计划

> **执行要求：** 按测试先行逐项实现，完成一项后运行目标测试，再进入下一项。

**目标：** 实现 QA 发布登记、严格双样本复测、结果对比、问题关闭和可完整体验的演示数据。

**架构：** 复用现有 `RetestRun`、`RetestSample` 与评测 Provider。后端增加工作台查询和执行服务，前端增加独立发布与复测页面；演示脚本补齐完整数据关系，不引入新基础设施。

**技术栈：** FastAPI、SQLAlchemy、MySQL/SQLite、React、TypeScript、Vitest。

## 全局约束

- 真实业务必须同时满足历史回放与新增样本两组最低数量。
- 复测锁定原评测规则、Prompt、模型和阈值，保证前后可比较。
- 页面中文易懂，沿用现有视觉规范并兼容移动端。
- 单人维护、单台服务器、每日处理量少于一万条。

## 任务一：工作台查询与发布登记

**文件：**

- 修改：`backend/app/retest/service.py`
- 修改：`backend/app/retest/router.py`
- 新建：`backend/tests/integration/test_retest_workspace_api.py`

- [x] 先编写失败测试，断言工作台返回待发布 QA、复测任务概况和详情。
- [x] 先编写失败测试，断言发布登记保存发布人、时间和备注，并创建可追溯复测任务。
- [x] 实现列表、详情和发布接口，错误返回中文业务提示。
- [x] 运行 `./.venv/bin/pytest tests/integration/test_retest_workspace_api.py -q`。

## 任务二：严格双样本执行

**文件：**

- 修改：`backend/app/retest/service.py`
- 修改：`backend/app/retest/router.py`
- 修改：`backend/tests/integration/test_retest_workspace_api.py`
- 修改：`backend/tests/unit/test_retest_decision.py`

- [x] 先编写失败测试，断言样本不足时返回等待状态且不调用模型。
- [x] 先编写失败测试，断言样本充足时逐条生成评测结果并绑定到复测样本。
- [x] 先编写失败测试，断言仅当两组通过率均达标时关闭问题。
- [x] 实现样本刷新、Provider 评测、结果持久化、完成判定和审计。
- [x] 运行复测单元及集成测试。

## 任务三：前端发布与复测工作台

**文件：**

- 新建：`frontend/src/app/retestApi.ts`
- 新建：`frontend/src/app/retestApi.test.ts`
- 新建：`frontend/src/features/retests/RetestWorkspacePage.tsx`
- 新建：`frontend/src/features/retests/RetestWorkspacePage.test.tsx`
- 修改：`frontend/src/app/router.tsx`
- 修改：`frontend/src/styles/theme.css`

- [x] 先编写 API 失败测试，覆盖列表、发布、刷新样本和执行复测请求。
- [x] 先编写页面失败测试，覆盖待发布、等待样本、执行入口和三组通过率对比。
- [x] 实现 API 类型、工作台交互、加载和错误状态。
- [x] 将 `/retests/*` 占位页替换为真实页面并补充响应式样式。
- [x] 运行前端目标测试和构建。

## 任务四：演示数据与最终验证

**文件：**

- 新建：`backend/app/demo/retest_data.py`
- 新建：`backend/scripts/加入发布复测演示数据.py`
- 新建：`backend/tests/unit/test_retest_demo_data.py`
- 修改：`使用说明.md`

- [x] 先编写失败测试，断言重复执行脚本仍只保留一套完整演示闭环数据。
- [x] 实现可重复运行且与现有 QA 演示完全隔离的演示数据脚本。
- [x] 在使用说明补充页面操作步骤和脚本命令。
- [x] 运行后端目标测试和 Ruff，并运行前端全量测试、前端构建和 `git diff --check`。
- [x] 启动页面后实际验证待发布、双样本准备条件和页面展示。

---

## 第二阶段：复测过程与逐条明细

### 任务五：后端只读复测明细

**文件：**

- 新建：`backend/app/retest/detail.py`
- 修改：`backend/app/retest/router.py`
- 修改：`backend/tests/integration/test_retest_workspace_api.py`

**接口：**

- 输入：`retest_detail(session: Session, run_id: str) -> dict`
- 输出：任务结论、方法说明、两个样本组统计和 `samples` 逐条结果。
- 约束：只读取现有 `RetestSample`、`Conversation`、`EvaluationResult` 和版本记录，不调用 Provider。

- [ ] **步骤 1：编写失败测试，覆盖完成和待评测样本**

```python
detail = client.get(f"/api/retest-workspace/{run_id}")
sample = detail.json()["samples"][0]
assert sample["cohort"] in ("replay", "new")
assert sample["conversation"]["messages"]
assert sample["evaluation"]["reason"]
assert detail.json()["explanation"]["formula"] == "历史回放和新对话两组通过率都达标"
```

- [ ] **步骤 2：运行测试并确认因缺少 `samples` 失败**

运行：`./.venv/bin/pytest tests/integration/test_retest_workspace_api.py -q`

- [ ] **步骤 3：实现只读明细组装**

```python
def retest_detail(session: Session, run_id: str) -> dict:
    summary = retest_workspace_detail(session, run_id)
    samples = _sample_details(session, run_id)
    return {**summary, "explanation": _explain(summary, samples), "samples": samples}
```

逐条结果必须包含：`cohort`、`occurred_at`、`conversation.messages`、`status`、`total_score`、`dimension_scores`、`passed`、`reason`、`evidence`、`confidence`、`severe_factual_error`、`severe_compliance_error`。

- [ ] **步骤 4：让详情路由调用 `retest_detail`，并验证查看详情不调用模型**

使用会在 `evaluate()` 时抛错的 Provider 覆盖依赖，连续请求详情两次，均应返回 `200`。

- [ ] **步骤 5：运行后端目标测试和 Ruff**

运行：

```bash
./.venv/bin/pytest tests/integration/test_retest_workspace_api.py -q
./.venv/bin/ruff check app tests
```

### 任务六：独立复测详情页

**文件：**

- 修改：`frontend/src/app/retestApi.ts`
- 修改：`frontend/src/app/retestApi.test.ts`
- 修改：`frontend/src/app/router.tsx`
- 新建：`frontend/src/features/retests/RetestDetailPage.tsx`
- 新建：`frontend/src/features/retests/RetestDetailPage.test.tsx`
- 修改：`frontend/src/styles/theme.css`

**接口：**

- 输入：`getRetestDetail(runId: string): Promise<RetestDetail>`。
- 路由：`/retests/:runId`。
- 页面不提供执行接口，只呈现已持久化结果。

- [ ] **步骤 1：编写 API 与页面失败测试**

```tsx
expect(html).toContain("为什么得到这个结论");
expect(html).toContain("历史回放 1/1 通过");
expect(html).toContain("评测理由");
expect(html).toContain("准确性");
```

- [ ] **步骤 2：运行测试并确认详情类型、组件和路由尚不存在**

运行：`npm test -- --run src/app/retestApi.test.ts src/features/retests/RetestDetailPage.test.tsx`

- [ ] **步骤 3：实现详情类型、加载状态和中文错误页**

```ts
export function getRetestDetail(runId: string): Promise<RetestDetail> {
  return request(`/api/retest-workspace/${runId}`);
}
```

- [ ] **步骤 4：实现结论解释、方法说明和逐条样本卡片**

默认排序为未通过、低分、待评测、已通过；支持“全部 / 历史回放 / 发布后新对话”筛选。证据、严重错误和维度分没有数据时显示明确空状态，不制造默认分数。

- [ ] **步骤 5：实现响应式样式并运行目标测试和构建**

运行：

```bash
npm test -- --run src/app/retestApi.test.ts src/features/retests/RetestDetailPage.test.tsx
npm run build
```

### 任务七：工作台方法摘要与完整验收

**文件：**

- 修改：`frontend/src/features/retests/RetestWorkspacePage.tsx`
- 修改：`frontend/src/features/retests/RetestWorkspacePage.test.tsx`
- 修改：`frontend/src/styles/theme.css`
- 修改：`使用说明.md`

**接口：**

- 输入：现有 `RetestItem.locked_rule`、两组样本数量和状态。
- 输出：“怎么复测”摘要与 `/retests/{id}` 明细入口。

- [ ] **步骤 1：编写失败测试，断言工作台能解释复测方法**

```tsx
expect(html).toContain("怎么复测");
expect(html).toContain("两组都达到 80% 才算改善有效");
expect(html).toContain("查看复测明细");
```

- [ ] **步骤 2：实现方法摘要和详情入口**

摘要只展示最关键的样本、裁判和判定三行；逐条理由放在详情页，避免任务卡片过长。

- [ ] **步骤 3：补充使用说明并执行最终验证**

运行：

```bash
./.venv/bin/pytest tests/unit/test_retest_decision.py tests/unit/test_retest_demo_data.py tests/integration/test_retest_workspace_api.py -q
./.venv/bin/ruff check app tests scripts
npm test -- --run
npm run build
git diff --check
```

- [ ] **步骤 4：实际打开演示任务详情**

验收：能看到两组样本、原对话、总分、维度分、理由和证据；反复刷新详情不新增 `EvaluationResult`，不产生模型请求。
