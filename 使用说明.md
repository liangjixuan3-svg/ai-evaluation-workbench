# AI 评测迭代工作台使用说明

项目不会自动启动，也不会在后台常驻。每次使用时由你手动启动，用完后手动停止。

本文的“项目根目录”指同时包含 `backend`、`frontend` 和 `README.md` 的文件夹，与项目在电脑上的存放位置无关。在 IDEA 中打开这个文件夹。下面每段含 `cd backend` 或 `cd frontend` 的命令，都请在项目根目录新开一个终端后执行；不要在已经进入子目录的终端中重复执行。

## 第一次使用

需要先准备：

- MySQL 8.0 或更高版本。
- Python 3.12 或更高版本。
- NVM 和 Node.js 24.12.0。
- 一个 OpenAI-compatible 模型服务，提供 Base URL、API Key 和模型名。

在 MySQL 中创建数据库和账号。如果已经有可用数据库，直接跳过这段：

```sql
CREATE DATABASE workbench CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'workbench'@'127.0.0.1' IDENTIFIED BY 'workbench';
GRANT ALL PRIVILEGES ON workbench.* TO 'workbench'@'127.0.0.1';
FLUSH PRIVILEGES;
```

安装后端依赖：

```bash
cd backend
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install -e '.[dev]'
```

生成后端配置：

```bash
cp ../.env.example .env
```

编辑 `backend/.env`，至少确认下面四项：

```dotenv
DATABASE_URL=mysql+pymysql://workbench:workbench@127.0.0.1:3306/workbench?charset=utf8mb4
LLM_BASE_URL=https://your-provider.example/v1
LLM_API_KEY=your-api-key
LLM_MODEL=your-model-name
```

创建或升级数据表：

```bash
source .venv/bin/activate
alembic upgrade head
```

首次拉取包含“评测校准”功能的代码后，请在 `backend` 目录激活 `.venv`，由你手动执行一次 `alembic upgrade head`。以后只有拉取的更新带来新的数据库迁移时，才需要在启动后端前再执行；不是每次启动服务都要执行。项目不会自动执行迁移，也不会替你操作 MySQL。

安装前端依赖：

```bash
cd frontend
source ~/.nvm/nvm.sh
nvm install
nvm use
npm install
```

## 每次启动

先确认 MySQL 已启动，然后打开三个终端窗口。

### 终端一：启动后端 API

```bash
cd backend
source .venv/bin/activate
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

### 终端二：启动评测 Worker

```bash
cd backend
source .venv/bin/activate
python -m app.operations.worker
```

### 终端三：启动网页

```bash
cd frontend
source ~/.nvm/nvm.sh
nvm use
npm run dev
```

看到 Vite 启动成功后，打开 <http://127.0.0.1:5173>。

## 操作顺序

### 配置公司质量标准

1. 点击左侧“评测规则”，在标准库点击“上传公司标准”。
2. 选择 `.docx` 或含可复制文字的 `.pdf` 文件，最大 20 MB。暂时没有正式制度时，可先上传 [`示例数据/公司客服质量标准示例.docx`](./示例数据/公司客服质量标准示例.docx) 体验完整流程；其中规则和时效均为虚构演示数据，不能直接用于真实业务。
3. 点击“开始 AI 解析”，等待规则草稿出现。这里直接使用后端模型配置，不需要 Worker。
4. 检查通过阈值和五维权重，权重合计必须为 100%。
5. 对照每条规则下方的原文引用，修改不准确的规则并勾选“人工确认”。
6. 点击“保存草稿”；全部规则确认后，点击“发布为公司标准”。

AI 生成的规则默认全部未确认，未经人工确认不能发布。已发布版本会锁定，不能直接修改。

### 配置评测 Prompt

1. 点击左侧“评测规则”，再切换到“评测 Prompt”页签。
2. 首次打开会自动创建内置 V1；已发布版本只能查看，不能修改。
3. 点击“复制为新版本”，在草稿中修改模型的评测指令。
4. 点击“保存草稿”可以稍后继续；确认后点击“发布并锁定”。
5. 新版本发布后成为发起评测时的默认 Prompt，旧版本仍可查看和手动选择。

页面只能修改评测指令。五维输出字段、JSON 格式、原文证据和置信度校验由系统固定，不能通过 Prompt 页面关闭。

### 采集并评测对话

1. 在“数据采集”页上传 JSON，确认系统自动识别的字段映射。
2. 生成脱敏预览，确认时间、用户消息和 AI 回答正确。
3. 确认入库，然后选择抽样数量、策略、一个已发布的公司质量标准和评测 Prompt。
4. 阈值和五维权重由所选标准固定；启动后运行页会同时记录公司标准和 Prompt 版本。
5. 页面会自动更新进度和模型判定结果，模型按该版本规则评测。

在“评测规则”页面选择已发布标准后，点击锁定提示中的“查看 V版本号 完整规则”；也可以在评测运行页点击标准名称下方的“查看完整规则”。详情页会展示阈值、五维权重、评分说明、通用规则、场景规则和制度原文引用。该页面是只读历史版本，不会随以后发布的新标准变化。

### 校准评测结果

1. 按“每次启动”中的说明，由你手动启动后端 API、评测 Worker 和网页；完成一次真实评测并等待结果出现。
2. 点击左侧“评测校准”。系统会为当天准备最多 20 条待复核案例；当天重复进入只会打开同一批案例。
3. 在左侧选择一条案例，查看对话、模型分数、模型理由，以及它为什么被抽到复核队列。
4. 如果认同模型判定，填写审核人，点击“认同模型判定”，再在确认框中提交。
5. 如果不认同，点击“不认同”，选择正确结论和主要分歧维度，填写人工依据后提交。该案例会自动显示“已加入回归案例”。
6. 在页面顶部查看今日进度、人机一致率和主要分歧维度；已复核案例会保留人工结论和回归标记，刷新页面后仍可查看。

校准抽样、浏览案例和提交人工复核都不会调用大模型，不消耗模型额度。人工结论独立保存，不会覆盖原始模型评测结果；只有不认同的案例会自动进入回归集合。

### 处理问题与确认归因

1. 完成一次包含失败案例的评测，点击左侧“问题与归因”。
2. 左侧按问题簇展示失败案例；点击一个问题，在右侧查看最多三条代表对话、分数和扣分理由。
3. 尚无归因建议时点击“生成 AI 归因建议”。只有这一步会追加一次模型调用，重复点击同一模型不会重复计费。
4. 核对 AI 建议，可将最终原因修改为知识缺失、理解错误、流程执行错误、服务态度或评测误报/其他。
5. 填写人工确认依据。中低置信度结果还需要勾选“我已检查代表样本”，再点击“确认归因并创建任务”。
6. 确认后系统按原因生成改进任务；知识缺失会生成 QA 审核任务，后续在“QA 审核”板块继续处理。

AI 归因只提供建议，不会自动替代人工判断。确认后的历史结果保持只读，避免后续修改破坏可追溯性。

### 审核并交付 QA

1. 在“问题与归因”确认一个问题属于“知识缺失”后，点击左侧“QA 审核”。
2. 页面默认展示待处理队列。点击左侧任务，先核对右侧的问题摘要和脱敏失败对话。
3. 状态为“待生成”时，点击“生成 QA 草稿”。只有这一步调用一次模型；同一任务重复操作不会再次调用。
4. 状态变为“待审核”后，逐项检查问题写法、标准回答、适用范围、处理步骤、预计时效和升级条件，可直接修改。
5. 在“业务依据”填写公司制度或文档名称，并粘贴支持该 QA 的原文。两项缺一不可，没有业务依据不能审核通过。
6. 内容正确时点击“审核通过并锁定”；版本锁定后可下载 JSON 或 CSV，交给知识库团队。
7. 内容不应采用时填写驳回原因并点击“驳回草稿”，系统会关闭任务并保留原因。

QA 草稿不会自动生成、自动审核或直接写入知识库。已通过和已驳回内容保持只读，保证后续可以追溯当时的审核结果。

### 登记发布并复测

1. 点击左侧“发布与复测”，在“待发布 QA”中核对审核答案。
2. 填写发布操作人和备注；确认内容已进入外部知识库后点击“登记发布”。
3. 系统自动准备历史回放样本和发布后的同场景新对话。新数据不足时点击“刷新样本”，页面会继续等待，不会提前关闭问题。
4. 两组样本都达到最低数量后，点击“开始复测”。复测直接调用后端配置的模型，不需要单独启动 Worker。
5. 页面会对比发布前、历史回放和新对话通过率。两组都达到锁定阈值时显示“改善有效”，否则显示“仍需改进”。
6. 在复测任务的“怎么复测”区域核对本次使用的样本范围、公司规则、Prompt、模型和判定条件。
7. 点击“查看复测明细”，可以查看结论计算方式，以及每条历史回放和新对话的原对话、总分、维度分、评测理由与引用证据。

“查看复测明细”只读取已经保存的复测结果，刷新或重复打开不会再次请求模型。待评测样本会明确显示“等待评测”，不会生成临时分数。

首次体验可以在后端目录执行 `python scripts/加入发布复测演示数据.py`，然后刷新网页。脚本可重复执行，不会重复加入数据。

如果服务在复测过程中被关闭，任务超过 30 分钟会显示“执行已中断”。确认旧进程已停止后点击“恢复并继续”，系统只处理尚未完成的样本。

如果暂时没有业务数据，可以先上传 `示例数据/客服对话示例.json` 体验流程。

## 停止

在三个运行中的终端里分别按 `Ctrl+C`。MySQL 是否停止由你自己决定，项目不会操作 MySQL 服务进程。

## 常见问题

### `crypto.getRandomValues is not a function`

说明当前仍在使用 Node.js 16。在 `frontend` 目录重新执行：

```bash
source ~/.nvm/nvm.sh
nvm use
node --version
```

版本应显示 `v24.12.0`。

### 页面提示“未配置 API”

检查 `backend/.env` 中的 `LLM_BASE_URL`、`LLM_API_KEY` 和 `LLM_MODEL`，修改后重启后端 API 和 Worker。

### 点击启动评测后一直没有结果

确认终端二的 Worker 正在运行，并查看该终端是否显示模型请求错误。
